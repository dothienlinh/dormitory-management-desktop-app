export enum UserRole {
  ADMIN = "admin",
  STUDENT = "student",
  STAFF = "staff",
}

export enum Gender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum UserStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  ABSENT = "absent",
}

export enum UserStatusAccount {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
  BANNED = "banned",
}
